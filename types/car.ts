export type TCarItem = {
    engine_type: string
    folder_id: string
    images: {
        image: string[]
    }
    mark_id: string
    modification_id: string
    price: number
    run: number
    unique_id: number
    year: number
}
